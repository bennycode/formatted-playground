{
  "license": "UNLICENSED",
  "main": "index.js",
  "name": "formatted-playground",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bennyn/formatted-playground.git"
  },
  "scripts": {
    "build": "yarn build:node && yarn build:browser",
    "build:browser": "exit 0",
    "build:node": "tsc",
    "start": "yarn build && node dist/ts/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "1.0.0",
  "dependencies": {
    "p-queue": "2.3.0"
  }
}
